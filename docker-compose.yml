version: '3.8'

services:
  sandbox:
    build: ./backend/sandbox
    ports:
      - "3003:3003"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - PORT=3003
    deploy:
      resources:
        limits:
          memory: 512M